"""
Messaging — NATS pub/sub infrastructure.

Responsibility: Provide NatsClient, EpisodeEventPublisher, and event schema
definitions. Acts as a pure infrastructure adapter — no business logic.

Allowed imports: nothing internal (infrastructure foundation layer).
"""
