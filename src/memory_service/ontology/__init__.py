"""
Ontology — domain vocabulary and entity name canonicalization.

Responsibility: Parse and expose the Schema.org JSON-LD taxonomy; provide
canonical entity name normalization used for consistent FalkorDB node IDs.

Allowed imports: nothing internal (foundation layer).
"""
