"""
Services — domain use cases and orchestration.

Responsibility: Implement all agent memory use cases (observe, startup,
curation, retrieval). Owns the MemoryService facade, the observe pipeline,
and pipeline configuration.

Allowed imports: storage/, intelligence/, ontology/.
Must NOT import from: transport/, workers/, messaging/.
"""
